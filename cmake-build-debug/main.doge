
//externals
extern int printFloat(float a);
extern float inF();
extern int printC(string a);
extern int newLine();
extern float pow(float base, float exp);



float pi(float n) {
    float sum = 0.0;
    float sign = 1.0;
    for (float i = 0.0; i < n; i = i + 1.0) {
        sum =  sum + sign/(2.0*i+1.0);
        sign = sign * -1.0;
    }
    return 4.0*sum;
}

//main function
int DogeMain(){
printC("Enter precision: ");
float in = inF();
newLine();
printFloat(pi(in));
return 0;

}
