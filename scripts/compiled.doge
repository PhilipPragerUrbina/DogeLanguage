//example compiled doge program

//externals
var printFloat(a){}
var inF(){}
var printC(a){}

//test function
float diff(a,b){
    return a - b;
}

//main function
var DogeMain(){
    //get start var
    printC("Start: ");
    float a = inF();

    //get end var
    printC("End: ");
    float b= inF();

    //loop
    float a_init = a;
    printFloat(a - 1.0);
     printFloat(a);
    while(a < b){
        a =  a + 1.0;
        printFloat(a);
    }

    //return
    return diff(a_init, a);

}


