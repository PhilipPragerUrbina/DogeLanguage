//externals
var printFloat(a){}
var inF(){}
var printC(a){}


//main function
var DogeMain(){
    float a = inF();
    float b = inF();
    if(a == b){
        printC("Equal!");
    }else{
     if(a != b){
            printC("not equal!");

             if(a < b){
                  printC("less than!");
             }

             if(a > b){
                   printC("greater than!");
             }

        }

    }
  return a/b;

}
