#import SL;
#import Thread;

function newThread(num){
    for(int i = 0; i < num; i++){
        print("Thread: " + i );
    }
    return num / 2;
}

int id = thread(newThread, 200);

for(int i = 0; i < 200; i++){
    print("Non thread: " + i );
}

print("Thread out:" + getThread(id));
